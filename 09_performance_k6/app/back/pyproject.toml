[project]
name = "ticketpass-api"
version = "0.1.0"
description = "Backend API de alto rendimiento para venta de entradas - Flask + Gunicorn + Redis + PostgreSQL"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "flask>=3.1.3",
    "gunicorn>=23.0.0",
    "flask-sqlalchemy>=3.1.1",
    "psycopg2-binary>=2.9.10",
    "pydantic[email]>=2.13.4",
    "python-dotenv>=1.2.2",
    "redis>=8.0.1",
]

[project.scripts]
serve = "main:create_app"
seed = "seed:seed_data"
